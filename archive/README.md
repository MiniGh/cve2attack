# Archive

Inactive pre-refactor scripts and the historical TF-IDF implementation are retained here for provenance. They are not imported by the active `src/cve2attack` package.

The former ignored top-level `bak/` directory is preserved as the ignored `archive/local_bak/` directory so it no longer obscures the active project layout.
